[project]
name = "pepit-ui"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "matplotlib>=3.10.8",
    "pepit",
    "plotly>=6.5.0",
    "pre-commit>=4.5.1",
    "streamlit>=1.52.2",
    "streamlit-ace>=0.1.1",
    "sympy>=1.13.0",
]

[tool.uv.workspace]
members = [
    "modules/PEPit",
]

[tool.uv.sources]
pepit = { workspace = true, editable = true }
